{
  "name": "bazaar",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@bazaar/backend",
    "dev:admin": "npm run dev --workspace=@bazaar/admin",
    "dev:storefront": "npm run dev --workspace=@bazaar/storefront",
    "dev:landing": "npm run dev --workspace=@bazaar/landing",
    "build": "npm run build --workspace=@bazaar/backend",
    "test": "npm test --workspace=@bazaar/backend",
    "prisma:migrate": "npm run prisma:migrate --workspace=@bazaar/backend",
    "prisma:generate": "npm run prisma:generate --workspace=@bazaar/backend",
    "prisma:seed": "npm run prisma:seed --workspace=@bazaar/backend"
  }
}
